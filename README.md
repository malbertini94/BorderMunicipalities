# BorderMunicipalities
In this repository I provide code to compute the distance between the centroid of polygons (e.g. municipalities) and objects in other specific coordinates (e.g custom officies).
